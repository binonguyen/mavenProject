pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                script {
                    def name = 'Bino'

                    if (name == 'Bino')
                        println("Hi ${name}")
                    else
                        println("Hi human")
                    echo "End here"
                }
            }
        }
    }
}
